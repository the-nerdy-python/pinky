type Rat @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  sex: String!
  status: RatStatus!
  size: RatSize!
}

enum RatStatus {
  alive
  frozen
  breeder
}

enum RatSize {
  pinky
  fuzzy
  pup
  weaned
  small
  medium
  large
  x-large
  breeder
}